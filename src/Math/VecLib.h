#ifndef __VECTOR_OPERATIONS__
#define __VECTOR_OPERATIONS__

#include <vector>
#include <cmath>

std::vector<double> NumProduct(const std::vector<double> &, const double a);
double DotProduct(const std::vector<double> &, const std::vector<double> &);

#endif